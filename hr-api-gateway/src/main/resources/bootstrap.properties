#configura o nome da aplicação para o server discovery
spring.application.name=hr-api-gateway-zuul

#configura a uri do servidor de configuração
spring.cloud.config.uri=http://localhost:8888

#Permite q o actuator atualize as configs durante runtime
management.endpoints.web.exposure.include=*